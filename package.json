{
  "name": "instagram-car-bot",
  "version": "1.0.0",
  "description": "Posts hourly luxury car photos to Instagram via the Graph API",
  "type": "module",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "test:once": "node --env-file=.env bot.js --once"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "libsodium-wrappers": "^0.8.4"
  }
}
